<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.ev.model.BoardMapper">
    
     <select id="showBoard" resultType="kr.ev.model.BoardVO">
        select b_title, b_file, m_nick from t_board
    </select>
    
     <insert id="boardInsert" parameterType="kr.ev.model.BoardVO">
         insert all
      into t_board (b_title, b_content, b_file) values (#{b_title}, #{b_content}, #{b_file}
      into t_board (m_nick) select #{m_nick} from t_member
     </insert>
</mapper>